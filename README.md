venv
```
python -m venv .venv
source .venv/bin/activate
```

requirements
```
pip install -r requirements.txt
```

might need to source again before running for the first time?

run with
```
uvicorn main:app --reload
```
